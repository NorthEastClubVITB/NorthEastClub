# -----------------------------------------------------------------------------
# Frontend Environment Variables Template
# -----------------------------------------------------------------------------
# Copy this file to '.env' or add these variables to your deployment platform 
# (e.g., Vercel, Netlify, Render, AWS Amplify) in the Environment Settings.
# -----------------------------------------------------------------------------

# Backend API URL
# When deploying the backend (e.g., to Render/Heroku), paste the live backend URL here.
# Make sure to include '/api' at the end if your backend routes require it!
# Example: https://my-backend-server.onrender.com/api
REACT_APP_API_URL=http://localhost:5001/api
